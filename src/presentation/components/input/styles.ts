import Styled from 'styled-components'

export const Container = Styled.div`
  input {
    flex: 1;
    background: transparent;
    border: 0;

    &::placeholder {
      color: #A4A4B2;
    }
  }
    svg {
      margin-right: 16px;
    }
  }
`
